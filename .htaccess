RewriteEngine On
RewriteBase /

# Remove .html from all URLs
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Redirect index.html to root (clean homepage)
RewriteRule ^index\.html$ / [R=301,L]